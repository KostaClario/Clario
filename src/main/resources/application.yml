server:
  port: 8883
  servlet:
    session:
      tracking-modes: cookie

spring:
  application:
    name: Clario
  security:
    oauth2:
      client:
        registration:
          google:
            client-id:
            client-secret:
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope:
              - email
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://34.64.69.172:3306/clario?characterEncoding=UTF-8
    username: cla
    password: clapw

  jpa:
    open-in-view: true
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false
    check-template-location: true


mybatis:
  mapper-locations: classpath:mappers/*.xml

logging:
  level:
    org.springframework.security: DEBUG
